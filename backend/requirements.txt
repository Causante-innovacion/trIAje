# GPT Legal - Backend Dependencies

# Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0  # SQLite async (desarrollo)
asyncpg>=0.29.0    # PostgreSQL async (producción)

# AI Providers
openai>=1.10.0
anthropic>=0.18.0

# Vector Store - Qdrant
qdrant-client>=1.7.0

# LangChain (RAG Framework)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20

# Embeddings (local - HuggingFace, alineado con RAG/chroma_db)
sentence-transformers>=2.2.0
torch>=2.0.0

# PDF Processing (para ETL de documentos legales)
PyMuPDF>=1.23.0

# DOCX Processing (para extracción de Plan Estratégico)
python-docx>=1.1.0

# File Upload (FastAPI)
python-multipart>=0.0.6

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0

# PDF/Doc export (para advisor package)
# jinja2>=3.1.0
# weasyprint>=60.0  # Para PDF

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Development
black>=24.0.0
ruff>=0.1.0
mypy>=1.8.0
